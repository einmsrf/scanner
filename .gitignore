# 编译产物
/scanner
/scanner.exe
/dist/

# 测试与覆盖率
*.test
*.out
coverage.html

# 本地配置（含 api_key，绝不入库）
/config.yaml

# 本地缓存（FingerprintHub 源码克隆、测试临时目录、本地演示目标）
/.cache/

# 扫描报告输出（默认目录相对当前工作目录，任意层级都忽略，避免扫描结果误入库）
reports/

# 编辑器
.idea/
.vscode/
